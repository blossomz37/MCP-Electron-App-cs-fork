# FictionLab 0.9.1 macOS signature repair

The user installed `downloads-update/FictionLab-0.9.1-arm64.dmg` and macOS reported the app as corrupt.

## Evidence

- DMG SHA256: `ac5cae3f3ab6e862f064715c8ca99b2a7ed75687140bc875480571acf3eeae6d`, exactly matching the GitHub release asset digest retrieved from the releases API.
- `hdiutil verify` passed.
- Installed app reported version 0.9.1, but `codesign --verify --deep --strict` failed with `code has no resources but signature indicates they must be present`.

This repeats the earlier release packaging/signature failure; the downloaded disk image is intact.

## Repair and backup

Original installed app preserved at:

`/Users/carlo/Library/Application Support/fictionlab/backups/v0.9.1-signature-20260918-193704/FictionLab.app`

Applied `codesign --force --deep --sign - /Applications/FictionLab.app`. Signature verification then passed. The first direct launch exited 137 without application log output. Removed the downloaded quarantine attribute only from this checksum-verified app using `xattr -dr com.apple.quarantine /Applications/FictionLab.app`, then launched with `open /Applications/FictionLab.app`.

## Verification and limits

- Native accessibility inspection confirms an open FictionLab window showing version 0.9.1 and the What's New dialog for 0.9.1.
- Startup logs at 19:37:59 show `2 total, 2 active, 0 errors` plugins.
- At 19:38:01 the updater reports `no update available (current: 0.9.1)`.
- Final deep/strict signature verification passed. No app code, settings, or manuscript files were edited during this repair.
- Existing plugin modifications from the earlier repair remain separate from the replacement app. The earlier 0.7.1 app-archive patches were not reapplied. Successful launch does not prove a future update-download failure is handled by this new version.

Local launch repair complete; app left open. The upstream macOS release still needs valid distribution signing/notarization. A future replacement may require the same local repair until packaging is corrected.
